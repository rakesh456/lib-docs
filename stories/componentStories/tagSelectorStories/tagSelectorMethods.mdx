
 
|Sr. No.|Methods|Comments|Examples|
|--- |--- |--- |--- |
|1|appendNewElement|Append new element to the list|var element = document.getElementById("idOfElement");<br/>element.appendNewElement({ "key": "Test", "value": "BPO" });|
|2|getSelectedValues|Get all the selected values|var element = document.getElementById("idOfElement");<br/>alert(element.getSelectedValues());|
|3|getSelectedCounter|Get count of selected elements|var element = document.getElementById("idOfElement");<br/>alert(element.getSelectedCounter());|
|4|getNewlyAdded|Get list of newly added elements into the list|var element = document.getElementById("idOfElement");<br/>alert(element.getNewlyAdded());|
|5|setSelectedItems|Sets the selected elements|var element = document.getElementById("idOfElement");<br/>element.setSelectedItems([{ "value": "Albania", "key": "AL" }, { "value": "Turkey", "key": "TR" }]);|
|6|setJsonData|Sets the Json data to be displayed as a list|var element = document.getElementById("idOfElement");<br/>element.setJsonData([{ "value": "Albania", "key": "AL" }, { "value": "Turkey", "key": "TR" }]);|
|7|remove|Remove list of selected elements|var element = document.getElementById("idOfElement");<br/>element.remove({ "key": "AL", "value": "Albania" });|
|8|refresh|Get the tag selector to its initial form|var element = document.getElementById("idOfElement");<br/>element.refresh();|